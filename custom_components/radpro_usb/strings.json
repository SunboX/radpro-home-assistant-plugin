{
  "title": "Rad Pro USB",
  "config": {
    "step": {
      "user": {
        "title": "Connect Rad Pro",
        "description": "Pick the USB serial port and polling settings.",
        "data": {
          "port": "Serial port",
          "baudrate": "Baud rate",
          "timeout": "Serial timeout (seconds)",
          "scan_interval": "Polling interval (seconds)",
          "commands": "Commands (comma-separated)",
          "enable_derived": "Enable derived CPS/CPM sensors"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Rad Pro options",
        "data": {
          "scan_interval": "Polling interval (seconds)",
          "commands": "Commands (comma-separated)",
          "enable_derived": "Enable derived CPS/CPM sensors"
        }
      }
    }
  }
}
